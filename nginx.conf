server {
  listen ${PORT};
  root /usr/share/nginx/html;
  index index.html;

  # Angular HTML5 routing - all routes fall back to index.html
  location / {
    try_files $uri $uri/ /index.html;
  }
}
